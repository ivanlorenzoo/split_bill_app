{
  "name": "split-bill-app",
  "version": "1.0.0",
  "description": "Aplikasi web untuk membagi tagihan bersama (client-side only)",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:coverage": "vitest --run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "fast-check": "^3.22.0",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
